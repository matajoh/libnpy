dist: bionic
language: cpp

addons:
  apt:
    update: true

before_install:
  - sudo apt-get install -y swig doxygen

script:
  - mkdir build
  - cd build
  - cmake -DCMAKE_BUILD_TYPE=RelWithDebInfo ..
  - cmake --build .
  - ctest -C RelWithDebInfo
